# Weather-app
🌦 Weather App Features Responsive design (mobile → desktop).  Search by city name.  Real‑time weather data via OpenWeather API.  Dynamic background (changes with weather condition).  Dark/Light mode toggle.  Error handling (invalid city names).  Clean, modern UI with CSS Flexbox/Grid.
